

import { Button } from "react-bootstrap";
import { Form } from "react-bootstrap";

function UpdateForm() {

    return(
    <div>
        <Form>

        <Button variant="success">Modificar Formulario</Button>{' '}

        </Form>
    </div>
    );
}
export default UpdateForm;